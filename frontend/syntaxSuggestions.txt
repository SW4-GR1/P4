

Data Types:
let int = 32 
let long_int = 64

let float = 0.32
let long_float = 0.64

Arithmetic operators:
+ - * / % ++ --

Logical operators:
and or not

Comprison operators:
== != < > <= >= 


assignment operators:
= += -= *= /=

Indentation:
Arbitrary, all whitespace ignored.
Seperate statements by ; or {}

bitwise operators: I vote no - magnus
bitAnd bitOr bitXor bitNeg

Data structures: 
array: let int[4] x = [x1,x2,x3,x4]; 
matrix: let matrix[2,2] mat; 
let vec[2] vec ==> matrix[2,1] vec //implicit description of vector not actually in lang



Build in functions // change pending:
Define matrix, takes matrix: mat.setMat([vec1, vec2]); ved ikke med det her, synes kun man skal kunne gøre det ved assignment operator
Add matrix: matAdd(mat1, mat2); 
Substrac matrix: matSub(mat1, mat2);
Scale matrix: matScale(mat, int);
Transpose matrix: matTrans(mat); 
Determinant matrix: matDet(mat);







---function declaration---
//i like this, looks more like mathematical function in header
dec f(int x, int y) : int {
  int res = x+y
  return res
}

func int (int x, int y) 
 if (x > 0)
  x + y
  done 
 x - y
 done 
    
    

---Loops---
for (int i = 0; i < 10; i++) {

}
====================

while (true) {

}
====================

do {

} while (true)
====================

for i in range of 1000 
for row in matrix
